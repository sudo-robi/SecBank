{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "prisma generate && tsc",
  "functions": {
    "api/index.ts": {
      "includeFiles": "public/**"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
